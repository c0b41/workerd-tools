addEventListener("fetch", (event) => {
    let request = event.request;
    const url = new URL(request.url);
    url.pathname = `/${BINDING_PLUGIN}/${BINDING_NAMESPACE}/${url.pathname}`;
    event.respondWith(BINDING_LOOPBACK.fetch(url, request));
});